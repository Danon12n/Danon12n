import {createSlice} from "@reduxjs/toolkit";

export const AuthRegRestSlice = createSlice( {
    name: "AuthRegRest",
    initialState: {
        show: false,
    },
    reducers: {
    }
});


export default AuthRegRest.reducer;

export const apiFunctions = {
}

