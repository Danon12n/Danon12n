import {createSlice} from "@reduxjs/toolkit";

export const WeaponCraftSlice = createSlice( {
    name: "WeaponCraft",
    initialState: {
        show: false,
    },
    reducers: {
    }
});


export default WeaponCraftSlice.reducer;

export const apiFunctions = {
}

